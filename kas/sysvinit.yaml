header:
  version: 9
  includes:
    - kas/base.yaml
    - kas/fit.yaml
    - kas/enclustra.yaml

local_conf_header:
  sysvinit: |
    DISTRO_FEATURES:remove = "systemd"
    DISTRO_FEATURES:append = " sysvinit initscripts"
    INIT_MANAGER = "sysvinit"
